## Plugin info template

The canonical template moved to:

- `../martix-template/dotnet-library/INFO.template.md`

This local file is kept as a compatibility pointer for older links.

## Usage

1. Copy the canonical template from `martix-template`.
2. Fill plugin-specific metadata, inventory, and requirements.
3. Add plugin-specific resources in `RESOURCES.md`.

